version: '3.4'

services:
  web.api:
    image: web.api
    build:
      context: ./src/Applications/Web/Web.Api
      dockerfile: Dockerfile

  web.mvc:
    image: web.mvc
    build:
      context: ./src/Applications/Web/Web.Mvc
      dockerfile: Dockerfile
    depends_on:
      - web.api