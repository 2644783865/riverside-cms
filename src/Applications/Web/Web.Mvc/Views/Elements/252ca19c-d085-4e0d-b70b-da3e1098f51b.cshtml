@using Riverside.Cms.Applications.Web.Mvc.Extensions
@using Riverside.Cms.Services.Element.Client

@model IElementViewModel<TableElementSettings, TableElementContent>

<div class="rcms-table">

    @if (Model.View.Settings.DisplayName != string.Empty)
    {
        <h2>@Model.View.Settings.DisplayName</h2>
    }

    @if (Model.View.Settings.Preamble != string.Empty)
    {
        <p class="rcms-preamble">@Html.FormatMultiline(Model.View.Settings.Preamble)</p>
    }

    <table class="table">
        @if (Model.View.Content.Headers != null)
        {
            <thead>
                <tr>
                    @foreach (string header in Model.View.Content.Headers)
                    {
                        <th>@header</th>
                    }
                </tr>
            </thead>
        }
        @if (Model.View.Content.Rows.Any())
        {
            <tbody>
                @foreach (IEnumerable<string> row in Model.View.Content.Rows)
                {
                    <tr>
                        @foreach (string cell in row)
                        {
                            <td>@cell</td>
                        }
                    </tr>
                }
            </tbody>
        }
    </table>

</div>